import type React from "react"
import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Accreditation - Golden Dynasty SA | Certifications & Standards",
  description: "View our professional accreditations, certifications, and compliance with industry standards.",
}

export default function AccreditationLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
